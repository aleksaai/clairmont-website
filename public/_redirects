# Customer-facing payment portal — proxied via a Netlify Function so the
# Supabase upstream's Content-Type and body pass through untouched.
# (A direct cross-origin 200! proxy is sandboxed by Netlify, breaking the page.)
/pay  /.netlify/functions/pay  200!

# SPA fallback for React Router routes
/*    /index.html   200
