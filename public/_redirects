# Customer-facing payment portal — must come BEFORE the SPA fallback.
# 200! = force redirect, GET and POST both pass through, query string preserved.
/pay  https://ufnxliieaejdvxcanqux.supabase.co/functions/v1/payment-portal  200!

# SPA fallback for React Router routes
/*    /index.html   200
